# Admin UI MVP Task Board

## Sprint 1 (Done)
- Add audit log viewer with filters and correlation links.

## Sprint 2 (Done)
- Add analytics page (top queries, no-result rate, click-through).
- Add search tuning surfaces (synonyms, boost rules, pinned results).

## Sprint 3 (Done)
- Add multi-tenant switcher and saved tenant presets.
- Add role-based UI gating once user accounts exist.

## Done
- Implement auth model 1 platform API key support for Admin API.
- Add Admin API read endpoints (tenants, api keys, allowed origins, ingestion jobs).
- Add Admin API CORS policy for local admin UI.
- Build Next.js admin UI shell with dashboard, imports, API keys, origins, tenants, and settings.
